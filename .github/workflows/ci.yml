name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  CI:
    runs-on: ubuntu-latest
    container: madninja/builder-erlang:2

    steps:
    - uses: actions/checkout@v2

    - name: Build
      run: make ci
